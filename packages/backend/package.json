{
  "name": "@cad/backend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/server.js",
    "test": "vitest run",
    "dev": "tsx watch --env-file=../../.env src/server.ts",
    "eval": "tsx --env-file=../../.env eval/runEval.ts"
  },
  "dependencies": {
    "@cad/shared": "workspace:*",
    "@jscad/modeling": "^2.12.0",
    "fastify": "^4.27.0",
    "@fastify/cors": "^9.0.0",
    "weave": "^0.14.0"
  },
  "devDependencies": {
    "tsx": "^4.10.0",
    "@types/node": "^20.0.0"
  }
}
